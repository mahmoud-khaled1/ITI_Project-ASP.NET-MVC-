
<!DOCTYPE html>
<html>
<head>
    <style>

        /*body {
            background-color: darkcyan;
        }*/
        /** {
            background: url(~/Content/images/2Bi8fJ.jpg);
            background-size: cover;
        }*/
        .na {
            text-decoration: none;
            color: white;
        }
    </style>
    <title>@Page.Title</title>
    @RenderSection("head", required: false)
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top" style="">
        <div class="container-fluid">
            <div class="navbar-header">
                @if (Session["u_name"] != null)
                {
                    <a class="navbar-brand" href="#">@Session["u_name"]</a>
                }
                else
                {
                    <a class="navbar-brand" href="#">Noname</a>
                }
            </div>
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Category", "Index", "User", null, new { @class = "ht" })</li>
                <li>
                    @*@if (Session["u_id"] != null)
                        {
                            @Html.ActionLink("Post An Ad", "CreatedAd", "User", null, new { @class = "ht" })

                        }*@
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    @if (Session["u_id"] == null)
                    {

                        @Html.ActionLink("Sign In", "login", "User", null, new { @class = "ht", })

                    }
                </li>
                <li>
                    @if (Session["u_id"] == null)
                    {
                        @Html.ActionLink("Sign Up", "SignUp", "User", null, new { @class = "ht" })
                    }
                    else
                    {
                        @Html.ActionLink("Sign Out", "Signout", "User", null, new { @class = "ht" })
                    }
                </li>
                <li>
                    @if (Session["u_id"] != null)
                    {
                        @Html.ActionLink("my cart", "cart", "User", new { @class = "ht" });
                    }

                </li>

            </ul>
        </div>


    </nav>

    @RenderBody()
   

    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="~/Scripts/bootstrap.js"></script>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
